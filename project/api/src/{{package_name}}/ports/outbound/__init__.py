"""Outbound (driven) port protocols.

Reserved for the interfaces the domain depends on for infrastructure (databases,
HTTP clients, model/LLM backends, queues). Empty until the first integration;
see docs/architecture.md for how to add one and extend the import-linter config.
"""
