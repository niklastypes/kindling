"""Inbound (driving) port protocols.

Reserved for explicit use-case contracts that inbound adapters call into. Empty
for now: the HTTP routes call domain services directly, which is the right shape
for trivial use cases. Add a protocol here when a use case earns one.
"""
